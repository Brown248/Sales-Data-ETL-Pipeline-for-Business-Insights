# Sales Data ETL Pipeline for Business Insights

โปรเจกต์นี้เป็นการสร้าง **ETL Pipeline (Extract, Transform, Load)** ด้วย Python เพื่อวิเคราะห์ข้อมูลยอดขาย (Sales Data) จากร้านค้าปลีก (Superstore) โดยมีเป้าหมายเพื่อทำความสะอาดข้อมูล คำนวณตัวชี้วัดสำคัญทางธุรกิจ และเตรียมข้อมูลสำหรับการสร้าง Dashboard หรือการวิเคราะห์ต่อด้วย SQL

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
├── data/
│   ├── raw/                  # เก็บไฟล์ข้อมูลดิบ (SampleSuperstore.csv)
│   └── processed/            # เก็บไฟล์ CSV ที่ผ่านการ Clean และสรุปผลแล้ว
├── database/
│   └── sales.db              # ฐานข้อมูล SQLite ที่เก็บข้อมูลพร้อมใช้งาน
├── notebooks/
│   └── analysis.ipynb        # Jupyter Notebook สำหรับทดลองวิเคราะห์ข้อมูล
├── scripts/
│   ├── extract.py            # สคริปต์สำหรับอ่านข้อมูล (Extract)
│   ├── transform.py          # สคริปต์สำหรับแปลงและทำความสะอาดข้อมูล (Transform)
│   ├── load.py               # สคริปต์สำหรับบันทึกข้อมูลลง Database/CSV (Load)
│   └── etl_pipeline.py       # สคริปต์หลักสำหรับรันกระบวนการทั้งหมด
├── sql/
│   └── analysis_queries.sql  # ชุดคำสั่ง SQL สำหรับดึง Insight ทางธุรกิจ
└── README.md

🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
Python 3.x

Pandas: สำหรับจัดการ Dataframe, ทำความสะอาด และคำนวณข้อมูล

SQLite3: สำหรับจัดเก็บข้อมูลในรูปแบบ Relational Database

Jupyter Notebook: สำหรับการวิเคราะห์เบื้องต้น (EDA)

⚙️ การทำงานของระบบ (Pipeline Workflow)
Extract: อ่านข้อมูลดิบจากไฟล์ data/raw/SampleSuperstore.csv

Transform:

เปลี่ยนชื่อ Column ให้เป็นมาตรฐาน (snake_case)

ตรวจสอบและจัดการข้อมูลสูญหาย (Missing Values) หรือข้อมูลซ้ำ (Duplicates)

Feature Engineering: สร้างตัวแปรใหม่ทางธุรกิจ

cost: ต้นทุนสินค้า (คำนวณจาก Sales - Profit)

profit_margin: อัตรากำไร (Profit / Sales)

สร้างตารางสรุปผล (Summary Tables) เช่น ยอดขายราย Category, Region, และ Segment

Load:

บันทึกข้อมูลหลักลงในฐานข้อมูล database/sales.db (Table: sales_data)

ส่งออกไฟล์สรุปผลไปยังโฟลเดอร์ data/processed/

🚀 วิธีการใช้งาน (How to Run)
ติดตั้ง Library ที่จำเป็น

Bash

pip install pandas
รัน ETL Pipeline เปิด Terminal หรือ Command Prompt แล้วรันคำสั่ง:

Bash

python scripts/etl_pipeline.py
เมื่อรันเสร็จสิ้น ระบบจะสร้างไฟล์ผลลัพธ์ใหม่ในโฟลเดอร์ data/processed/ และอัปเดตฐานข้อมูล sales.db

การวิเคราะห์ด้วย SQL คุณสามารถนำไฟล์ database/sales.db ไปเปิดในโปรแกรมเช่น DB Browser for SQLite และใช้คำสั่งจากไฟล์ sql/analysis_queries.sql เพื่อดู Insight ต่างๆ ได้ทันที เช่น:

ยอดขายรวมและกำไรสุทธิ

สินค้าที่ทำกำไรสูงสุด Top 5

ประสิทธิภาพยอดขายรายภูมิภาค

📊 ตัวอย่าง Insight ที่ได้ (Sample Insights)
Profitability: วิเคราะห์ได้ว่าสินค้าหมวดหมู่ไหนทำกำไรได้ดีที่สุด

Regional Analysis: เปรียบเทียบยอดขายในแต่ละภูมิภาค

Discount Impact: วิเคราะห์ผลกระทบของการให้ส่วนลดต่อกำไรสุทธิ

---

### สิ่งที่ผมเพิ่มให้ใน README นี้:
1.  **Project Structure:** แสดงภาพรวมไฟล์ให้คนอื่น (หรือตัวเราในอนาคต) เข้าใจง่าย
2.  **Workflow:** อธิบายตรรกะว่าโค้ดทำอะไรบ้าง โดยอิงจากสิ่งที่เราคุยกัน (เช่นการสร้าง Cost, Profit Margin)
3.  **How to Run:** ขั้นตอนการติดตั้งและสั่งรันโปรแกรม
4.  **SQL Integration:** เชื่อมโยงให้เห็นว่าไฟล์ SQL ที่มีในโปรเจกต์เอาไว้ทำอะไร