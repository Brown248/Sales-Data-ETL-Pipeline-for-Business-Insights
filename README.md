# Sales Data ETL Pipeline for Business Insights

โปรเจกต์ Python สำหรับทำ ETL (Extract, Transform, Load) ข้อมูลยอดขาย Superstore เพื่อทำความสะอาดข้อมูล คำนวณกำไร/ต้นทุน และจัดเตรียมข้อมูลลงฐานข้อมูล SQLite สำหรับการวิเคราะห์

## 📂 โครงสร้างโปรเจกต์
* `data/`: เก็บไฟล์ CSV (Raw และ Processed)
* `database/`: เก็บฐานข้อมูล SQLite (`sales.db`)
* `scripts/`: โค้ด Python สำหรับรันระบบ (`extract`, `transform`, `load`)
* `sql/`: ชุดคำสั่ง SQL สำหรับดึง Insight

## 🚀 วิธีใช้งาน (Quick Start)

1.  **ติดตั้ง Library**
    ```bash
    pip install pandas
    ```

2.  **รันระบบ (Run Pipeline)**
    ```bash
    python scripts/etl_pipeline.py
    ```
    *ระบบจะอ่านไฟล์ CSV, คำนวณ Cost/Profit Margin, และบันทึกผลลัพธ์ลง `data/processed/` และ `sales.db` ทันที*

3.  **การวิเคราะห์ข้อมูล**
    * **SQL:** ใช้ไฟล์ `sql/analysis_queries.sql` รันกับไฟล์ฐานข้อมูล `database/sales.db` (แนะนำใช้โปรแกรม *DB Browser for SQLite*)
    * **Python:** ดูตัวอย่างการวิเคราะห์ได้ใน `notebooks/analysis.ipynb`

## ✨ สิ่งที่ระบบทำ
* ✅ **Clean Data:** ลบข้อมูลซ้ำ, จัดการชื่อ Column ให้เป็นมาตรฐาน
* ✅ **Feature Engineering:** เพิ่มตัวแปร `cost` (ต้นทุน) และ `profit_margin` (อัตรากำไร)
* ✅ **Export:** ส่งออกข้อมูลพร้อมใช้ทั้งแบบ CSV และ SQL Database